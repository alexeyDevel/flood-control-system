import { Box, styled } from "@mui/material";

export const StyledBox = styled(Box)({
  border: "1px solid #00b274", // Цвет рамки
  borderRadius: 12, // Закругление углов
  padding: 16, // Отступы
});
